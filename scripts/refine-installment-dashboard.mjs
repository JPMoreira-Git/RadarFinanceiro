import fs from 'node:fs';
const path = '/home/ubuntu/controle-financeiro-familiar/client/src/pages/Home.tsx';
let s = fs.readFileSync(path, 'utf8');

s = s.replace(/\nconst monthlyData = \[[\s\S]*?\n\];\n/, `\nfunction buildMonthlyData(transactions: Transaction[]) {\n  const months = [\n    ["2026-03", "Mar"], ["2026-04", "Abr"], ["2026-05", "Mai"],\n    ["2026-06", "Jun"], ["2026-07", "Jul"], ["2026-08", "Ago"],\n  ] as const;\n  return months.map(([key, month]) => {\n    const entries = transactions.filter((item) => item.date.startsWith(key));\n    return {\n      month,\n      receita: entries.filter((item) => item.type === "receita").reduce((sum, item) => sum + item.amount, 0),\n      investimentos: entries.filter((item) => item.subcategory === "Rendimento de investimentos").reduce((sum, item) => sum + item.amount, 0),\n      gastos: entries.filter((item) => item.type === "despesa").reduce((sum, item) => sum + item.amount, 0),\n    };\n  });\n}\n`);
s = s.replace('  const { receita, despesas, investimentos, cobertura: coverage } = summarizeTransactions(monthTransactions);\n  const recent = transactions.slice(0, 4);', '  const { receita, despesas, investimentos, cobertura: coverage } = summarizeTransactions(monthTransactions);\n  const salary = monthTransactions.filter((item) => item.subcategory === "Salário").reduce((sum, item) => sum + item.amount, 0);\n  const monthlyData = buildMonthlyData(transactions);\n  const recent = transactions.slice(0, 4);');
s = s.replace('value: 9200, tone: "positive"', 'value: salary, tone: "positive"');
s = s.replace('value: investimentos + 9200 - despesas', 'value: investimentos + salary - despesas');

s = s.replace('function TransactionsView({ transactions, onDelete, onUpdate, categoriesData, payments }: { transactions: Transaction[]; onDelete: (id: number) => void; onUpdate: (transaction: Transaction) => void; categoriesData: Record<string, string[]>; payments: string[] }) {', 'function TransactionsView({ transactions, onDelete, onDeleteGroup, onUpdate, categoriesData, payments }: { transactions: Transaction[]; onDelete: (id: number) => void; onDeleteGroup: (groupId: string) => void; onUpdate: (transaction: Transaction) => void; categoriesData: Record<string, string[]>; payments: string[] }) {');
s = s.replace('  const filtered = transactions.filter((item) => item.date.startsWith(month) && (filter === "Todos" || item.category === filter) && (responsible === "Todos" || item.responsible === responsible) && (payment === "Todos" || item.payment === payment) && `${item.category} ${item.subcategory} ${item.note}`.toLowerCase().includes(search.toLowerCase()));', '  const filtered = transactions.filter((item) => item.date.startsWith(month) && (filter === "Todos" || item.category === filter) && (responsible === "Todos" || item.responsible === responsible) && (payment === "Todos" || item.payment === payment) && `${item.category} ${item.subcategory} ${item.note}`.toLowerCase().includes(search.toLowerCase()));\n  const displayItems = Array.from(new Map(filtered.map((item) => [item.installmentGroupId ?? `single-${item.id}`, item])).values());');
s = s.replace('{filtered.map((item) => editingId === item.id ?', '{displayItems.map((item) => editingId === item.id ?');
s = s.replace('<button onClick={() => onDelete(item.id)} className="rounded-lg p-2 text-[#83938b] hover:bg-[#f8e8e5] hover:text-[#a55348]" aria-label="Excluir"><Trash2 className="h-4 w-4" /></button>', '<button onClick={() => item.installmentGroupId ? onDeleteGroup(item.installmentGroupId) : onDelete(item.id)} className="rounded-lg p-2 text-[#83938b] hover:bg-[#f8e8e5] hover:text-[#a55348]" aria-label={item.installmentGroupId ? "Excluir compra parcelada" : "Excluir"} title={item.installmentGroupId ? "Excluir todas as parcelas" : "Excluir"}><Trash2 className="h-4 w-4" /></button>');
s = s.replace('<span className="rounded-md bg-[#f1f5f2] px-2 py-0.5 text-[10px] font-semibold text-[#7c8d85]">{item.category}</span></div><p className="mt-1 truncate text-xs text-[#95a39c]">', '<span className="rounded-md bg-[#f1f5f2] px-2 py-0.5 text-[10px] font-semibold text-[#7c8d85]">{item.category}</span>{item.installmentGroupId && <span className="rounded-md bg-[#f5ecdf] px-2 py-0.5 text-[10px] font-semibold text-[#98633c]">Parcelado · {item.installmentCount}x</span>}</div><p className="mt-1 truncate text-xs text-[#95a39c]">');
s = s.replace('const deleteTransaction = (id: number) => { setTransactions((current) => current.filter((item) => item.id !== id)); toast.success("Lançamento removido do protótipo."); };', 'const deleteTransaction = (id: number) => { setTransactions((current) => current.filter((item) => item.id !== id)); toast.success("Lançamento removido."); };\n  const deleteTransactionGroup = (groupId: string) => { setTransactions((current) => current.filter((item) => item.installmentGroupId !== groupId)); toast.success("Compra parcelada removida."); };');
s = s.replace('<TransactionsView transactions={transactions} onDelete={deleteTransaction} onUpdate={updateTransaction} categoriesData={categoriesData} payments={payments} />', '<TransactionsView transactions={transactions} onDelete={deleteTransaction} onDeleteGroup={deleteTransactionGroup} onUpdate={updateTransaction} categoriesData={categoriesData} payments={payments} />');
fs.writeFileSync(path, s);
